import queue from '.';

// test('basic', () => {
//   expect(sum()).toBe(0);
// });

test('basic again', () => {
  expect(1 + 2).toBe(3);
});

test('basic again', () => {

  const testQueue = queue('test');

  expect(testQueue).to

  expect(1 + 2).toBe(3);
});
